from collections import deque
from typing import *


def solution(rectangle: List[List[int]], characterX: int, characterY: int, itemX: int, itemY: int):
    """
    기본적인 아이디어는 전체 사각형들을 아우르는 지도에서 길찾기를 하는 것과 같음
    그저 유효한 길인지 판정하는 함수만 추가로 필요
    """
    answer = 0

    return answer


def is_valid(rectangle: List[List[int]], m, n) -> bool:
